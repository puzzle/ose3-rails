#!/bin/bash

set -ex

dir=$(dirname $0)

ls

if [ -e /tmp/src/.s2i/pre_assemble ]; then
  echo "Found pre_assemble script, executing it"
  /tmp/src/.s2i/pre_assemble
fi

$dir/assemble_rails

if [ -e /tmp/src/.s2i/post_assemble ]; then
  echo "Found post_assemble script, executing it"
  /tmp/src/.s2i/post_assemble
fi
