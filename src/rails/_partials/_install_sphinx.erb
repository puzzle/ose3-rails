USER root

# SLOW STUFF
# Slow operations, kept at top of the Dockerfile so they're cached for most changes.

# Install Sphinx SE from EPEL. 
RUN yum localinstall -y http://sphinxsearch.com/files/sphinx-2.2.11-1.rhel7.x86_64.rpm && \
    yum clean all
